# The first checkpoints run without external services.
# AWS and model settings will be added only when those adapters are introduced.
MANIFEST_MODE=shadow
DEMO_APPROVER_SECRET=replace-with-a-local-demo-value
